function introFade() {
    let intro = document.querySelector('.intro')
    if (intro.className === "intro") {
        intro.className += " introPressed";
    }
    else {
        className = "intro"
    }
}
